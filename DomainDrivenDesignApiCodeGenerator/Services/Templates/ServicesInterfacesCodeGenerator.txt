using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Microsoft.AspNetCore.JsonPatch;
${namespaces}

namespace ${namespace}
{
    public interface I${className}Service
    {
        Task CreateAsync(${createMethodParams});

        Task UpdateAsync(Guid id, JsonPatchDocument<${className}Dto> jsonPatchUpdateDocument, Guid modifedBy);

        Task DeleteAsync(Guid id, Guid modifiedBy);

        Task<${className}Dto> GetAsync(Guid id);

        Task<IEnumerable<${className}Dto>> GetListAsync(IPageQuery query);
    }
}